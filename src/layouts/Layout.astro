---
import "../styles/globals.css";
import SmoothScroll from "../components/SmoothScroll";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Making things, but with more intention" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <!-- Preload critical fonts to avoid render-blocking -->
    <link rel="preload" href="/fonts/PPLocomotiveNew-Light.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/HelveticaNowDisplay-Regular.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/HelveticaNowDisplay-Medium.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/HW-Cigars.woff2" as="font" type="font/woff2" crossorigin />
    <slot name="head" />
  </head>
  <body class="bg-transparent text-primary selection:bg-accent selection:text-white overflow-x-hidden">
    <SmoothScroll client:load />
    <slot />
  </body>
</html>
